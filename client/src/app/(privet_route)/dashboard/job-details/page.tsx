import { FC } from 'react'

interface pageProps {

}

const page: FC<pageProps> = ({ }) => {
    return <div>
        Testing page for id home page. And "Id" is missing
    </div>
}

export default page